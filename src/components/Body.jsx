const Body = ({ children }) => {
  return (
    <section className="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#e3f2fd] to-white">
      {children}
    </section>
  );
};

export default Body;
